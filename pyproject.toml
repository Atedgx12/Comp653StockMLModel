[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stockml"
version = "0.2.0"
description = "Generalized cross-asset prediction model for COMP 653 Statistical Machine Learning"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Zachary Powell", email = "zp21@rice.edu" },
    { name = "Josh Levy", email = "jl500@rice.edu" },
]

dependencies = [
    "numpy>=1.26",
    "pandas>=2.2",
    "pyarrow>=15",
    "scipy>=1.13",
    "scikit-learn>=1.5",
    "lightgbm>=4.3",
    "yfinance>=0.2.40",
    "matplotlib>=3.8",
    "tqdm>=4.66",
    "PyYAML>=6.0",
    "joblib>=1.4",
    "vaderSentiment>=3.3.2",
]

[project.optional-dependencies]
torch = [
    "torch>=2.3",
]
gpu = [
    "cupy-cuda12x>=13",
]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "ruff>=0.5",
    "mypy>=1.10",
    "ipykernel>=6.29",
    "jupyterlab>=4.2",
    "nbformat>=5.10",
]

[project.urls]
Homepage = "https://github.com/Atedgx12/Comp653StockMLModel"
Issues = "https://github.com/Atedgx12/Comp653StockMLModel/issues"

[project.scripts]
stockml = "stockml.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"stockml" = ["py.typed"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "PL", "RUF"]
ignore = ["PLR0913", "PLR0917", "PLR2004", "PLC0415", "PLR0915"]

[tool.ruff.lint.per-file-ignores]
# The course-network implementation intentionally keeps the lecture-style
# equations compact, and package initializers provide a public import surface.
"src/ucn/**/*.py" = ["E702", "PLR0912"]
"src/ucn/**/__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
strict_optional = true
ignore_missing_imports = true
follow_imports = "skip"
no_site_packages = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q --strict-markers"
markers = [
    "slow: marks tests as slow",
    "torch: tests that require pytorch",
]
