name: tcn
family: tcn
task: sequence_regression
requires_torch: true
params:
  input_window: 64
  channels: [64, 64, 64, 64]
  kernel_size: 3
  dropout: 0.1
  learning_rate: 1e-3
  weight_decay: 1e-5
  batch_size: 256
  epochs: 30
  early_stopping_patience: 5
